module.exports = {
  theme: {
    extend: {
      opacity: ['disabled'],
      cursor: ['disabled'],
      screens: {
        'print': {'raw': 'print'}
      }
    }
  },
  variants: {},
  plugins: [
    // require('@tailwindcss/forms'),
  ]
}
